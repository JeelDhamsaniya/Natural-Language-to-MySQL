# Example env for the React frontend
# Set this to the full backend API base URL (include /api if your backend uses it)
# Example: REACT_APP_API_URL=https://my-backend.example.com/api
REACT_APP_API_URL=https://<YOUR_BACKEND_URL>/api
